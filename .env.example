# Database
DATABASE_URL="mysql://root@localhost/java_repeater"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# Encryption key for sensitive data (generate with: openssl rand -base64 32)
ENCRYPTION_KEY="your-encryption-key-here-generate-with-openssl-rand-base64-32"

# OpenAI (optional - users can provide their own keys)
# OPENAI_API_KEY="sk-..."
